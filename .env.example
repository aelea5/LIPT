# Copy to .env and fill in your DreamHost MySQL credentials.
DB_HOST=your-mysql-host.dreamhost.com
DB_NAME=lunchinthepark
DB_USER=your_db_user
DB_PASS=your_db_password

# Optional: only if auto-detection is wrong (no leading/trailing slashes required)
# SITE_BASE=lunch-in-the-park
